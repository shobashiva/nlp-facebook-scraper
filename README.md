# nlp-facebook-scraper
A set of scripts to help you get Facebook post transformed to work with nltk


Setup

run pip install -r requirements.txt

After that, in the python shell, run `import nltk; nltk.download()`
